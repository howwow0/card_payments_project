spring:
  application:
    name: cpp-security-service

jwt:
  expirationTime: 900

server:
  port: ${CPP_SECURITY_SERVICE_PORT}
  servlet:
    context-path: /api/v1

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method

keydb:
  datasource:
    url: ${KEY_DB_URL}
    username: ${KEY_DB_USERNAME}
    password: ${KEY_DB_PASSWORD}
    card-name: ${CARD_NAME_KEY}
    jwt-name: ${JWT_NAME_KEY}

logging:
  level:
    org.springframework: INFO
    com.howwow: INFO
